apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: lab8

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - postgres-headless-svc.yaml
  - postgres-statefulset.yaml

commonLabels:
  app.kubernetes.io/managed-by: kustomize
  app.kubernetes.io/part-of: lab8-packaging